import { IProducts } from '../interfaces';
import { productMock } from './product.mock';

export const productsDataMock: IProducts = {
  count_category: 1,
  count_pages: 1,
  products: [productMock]
};
