export enum Pathname {
  Home = "/",
  Login = "/login",
  Orders = "/orders",
  Profile = "/profile",
  Registration = "/registration",
  ResetPassword = "/reset-password",
  Wishlist = "/wishlist",
}
