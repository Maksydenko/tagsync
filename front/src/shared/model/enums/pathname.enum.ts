export enum Pathname {
  Checkout = "/checkout",
  Comparisons = "/comparisons",
  Home = "/",
  Login = "/login",
  Orders = "/orders",
  Profile = "/profile",
  Registration = "/registration",
  ResetPassword = "/reset-password",
  Wishlist = "/wishlist",
}
