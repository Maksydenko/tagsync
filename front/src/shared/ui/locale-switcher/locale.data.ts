import { Locale } from '@/shared/config';
import { ILink } from '@/shared/model';

export const localeData: ILink[] = [
  {
    label: 'en',
    value: Locale.EN
  },
  {
    label: 'uk',
    value: Locale.UK
  }
];
